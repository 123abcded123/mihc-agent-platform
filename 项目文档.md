# 项目全流程教学文档

## 5. 项目一：医疗科研领域智能 Agent 平台

### 5.1 需求背景

医疗科研人员通常需要完成几类工作：查医学知识、找文献、理解检查检验结果、设计实验、分析科研数据。直接把问题交给通用大模型会产生几个问题：

- 模型不知道企业私有资料；
- 医疗术语和临床标准容易被混淆；
- 文献问题需要来源和引用；
- 多步骤任务容易中途丢失上下文；
- 患者信息和科研数据存在隐私风险；
- 没有日志就无法知道错误发生在检索、工具还是生成阶段。

项目目标不是训练一个“什么都知道”的模型，而是建立一个受控系统：识别问题类型，使用对应的知识和工具，生成有依据的结果，并允许评估和追踪。

### 5.2 端到端流程模拟

以问题“请检索 2020 年后关于肝细胞肿瘤和间质mihc的研究，并设计一个体外实验方案”为例：

```text
Vue3 对话页面
  -> FastAPI 接口
  -> 身份/权限/敏感信息检查
  -> Redis 读取会话上下文
  -> BERT 意图识别：文献检索 + 实验设计
  -> LangGraph 动态路由
  -> Query Rewrite / HyDE
  -> Elasticsearch 关键词检索 + Milvus 向量检索
  -> RRF 融合 + BGE-Reranker 重排
  -> 文献 Agent 整理证据
  -> 实验设计 Agent 生成方案
  -> Qwen3-32B 生成最终回答
  -> DFA + Prompt Guardrail 安全检查
  -> Langfuse/OpenTelemetry 记录链路
  -> 返回答案、引用、风险和 trace_id
```

### 5.3 知识库构建

原始资料可能包括 PDF、网页、Word、实验 SOP、临床检验标准和业务表。导入流程通常是：

```text
文件采集
 -> 格式解析
 -> OCR/表格识别（需要时）
 -> 清洗页眉页脚和重复文本
 -> 按标题、段落、表格切分
 -> 生成 embedding
 -> 写入 Milvus
 -> 关键词字段写入 Elasticsearch
 -> PostgreSQL 保存文档版本、来源和权限
```

一个文档片段至少应保存：

```json
{
  "chunk_id": "doc-001-chunk-03",
  "text": "文献或标准的内容片段",
  "title": "章节标题",
  "source": "原始文件或 DOI",
  "version": "2026-01",
  "permissions": ["research_team"],
  "embedding_model": "BGE-M3"
}
```

困难在于切分边界和权限。把一张完整表格拆散，检索时可能只拿到半行；把患者资料和公共知识放在同一权限范围，又可能造成隐私泄露。因此“万级文档”本身不是难点，难点是来源、版本、权限和可引用性。

### 5.4 检索流程细解

#### 第一步：查询改写

保留原始问题，同时生成适合检索的关键词和扩展问题。不要只保留改写结果，因为改写模型也可能误解用户意图。

#### 第二步：双路召回

- ES/BM25 找精确关键词，例如药物名、基因名、检测指标、年份；
- Milvus 找语义相似片段，例如“活性氧”与“ROS”的近义表达。

#### 第三步：融合排序

通过 RRF 合并两个结果列表。这样某个片段如果在关键词和语义结果中都靠前，就更有机会进入候选集。

#### 第四步：重排

BGE-Reranker 接收“问题 + 候选片段”，直接判断二者相关性，再选 Top-K 作为生成上下文。Reranker 不能创造证据，只能重新排列已有候选。

#### 第五步：生成

Prompt 规定模型必须：

- 只使用检索片段中的证据；
- 每个关键结论给出来源；
- 不确定时说明证据不足；
- 医疗建议和科研建议分开表达；
- 不把实验假设写成临床结论。

### 5.5 多 Agent 编排

一个合理的图结构：

```text
入口
 -> 意图识别
 -> 任务规划
 -> 文献 Agent / 知识问答 Agent / 检验解读 Agent / 数据分析 Agent
 -> 结果合并
 -> 引用检查
 -> 安全护栏
 -> 输出
```

任务规划要把复杂问题拆成可验证步骤。例如：

1. 找到相关文献；
2. 筛选年份、模型和实验对象；
3. 总结研究结论；
4. 根据证据设计实验；
5. 检查实验方案是否缺少对照组、重复数和终点指标。

常见困难：Agent 会重复调用工具、把检索结果当成事实、遗漏用户条件或生成无法执行的实验步骤。因此需要最大步数、工具超时、参数校验、失败重试和人工审核。

### 5.6 1.5 万条指令数据和微调

数据可能包含医疗问答、文献摘要、实验设计、数据解释和拒答样本。处理过程：

```text
收集历史问题
 -> 去重
 -> 去除个人隐私
 -> 专家撰写或审核答案
 -> 标注意图、难度、来源和安全等级
 -> 划分训练/验证/测试集
 -> SFT/LoRA
 -> 用偏好对做 DPO
 -> 离线评测
 -> 小流量上线
```

SFT 学“怎样回答”，DPO 学“两个回答哪个更符合偏好”。DPO 不是用来补充知识的唯一办法；知识更新更适合更新知识库。训练数据和评测数据必须隔离，否则 62% 到 98% 的提升可能只是记住了答案。

### 5.7 记忆、权限和安全

Redis 适合存短期会话状态、最近消息和任务进度；Milvus 适合保存需要语义检索的长期信息。两者不能简单等同于“记忆”。

多用户隔离的关键是每条记录附带 `tenant_id`、`user_id`、`session_id` 和权限过滤条件。查询时必须先做权限过滤，再做向量召回，不能召回后才凭模型判断是否允许展示。

DFA 是基于有限自动机的字符串匹配方法，可以高效发现敏感词。它不能识别所有隐含风险，所以还需要：

- 输入侧敏感信息检测；
- Prompt 注入防护；
- 工具权限控制；
- 输出侧敏感信息和越权内容检测；
- 审计日志和人工复核。

### 5.8 评估和监控

检索评估：`HitRate@K`、`Recall@K`、`MRR`。生成评估还要看：

- 忠实性：回答是否被证据支持；
- 完整性：是否覆盖问题关键点；
- 引用正确率：引用是否真的支持结论；
- 安全拒答率：危险问题是否正确拦截；
- 延迟、token 消耗和失败率。

Langfuse 记录 LLM 级别输入输出和 token；OpenTelemetry 贯通服务链路；Prometheus 采集可聚合指标；Grafana 展示趋势并告警。出现“回答变差”时，先根据 trace 判断是路由错、召回错、重排错、Prompt 错还是模型服务超时。

---

## 6. 项目二：大模型智研 Agent 平台

### 6.1 需求背景

企业内部信息通常分散在 CRM、业务系统、数据仓库、制度文档和项目文件中。员工想知道“某区域本季度销售额”“某研发项目进度”“某客户历史记录”，往往需要跨系统查询。平台要解决两类问题：

- 文档型问题：找制度、项目资料、研发知识；
- 数据型问题：按条件查询业务指标和经营数据。

最大的技术难点是：自然语言不等于数据库字段，模型生成的 SQL 也可能错误或危险。

### 6.2 平台流程

```text
用户问题
 -> FastAPI 鉴权
 -> 模型网关选择 DeepSeek/Qwen
 -> 意图判断：知识问答 or NL2SQL
 -> 文档检索 / 指标元数据检索
 -> Agent 规划
 -> 工具调用
 -> 结果校验和解释
 -> MongoDB 保存会话与评测数据
 -> Prometheus/Grafana 记录指标
```

### 6.3 模型网关

模型网关对上层提供统一接口，对下层屏蔽不同厂商模型的请求格式。它可以实现：

- 根据任务选择模型；
- 某个模型超时后切换备用模型；
- 按权重或负载分发请求；
- 统一 token、耗时和错误记录；
- 统一 Prompt 和返回格式。

教学伪代码：

```python
async def chat(messages, task_type):
    candidates = ["qwen", "deepseek"] if task_type == "analysis" else ["deepseek", "qwen"]
    last_error = None
    for provider in candidates:
        try:
            return await provider_client(provider).chat(messages, timeout=20)
        except TimeoutError as exc:
            last_error = exc
    raise RuntimeError("所有模型服务均不可用") from last_error
```

“错误率降至 0.5% 以下”必须说明统计的是 HTTP 错误、模型调用错误、Agent 任务失败，还是所有请求的综合失败率；还要说明时间窗口和流量规模。

### 6.4 企业知识库和元数据

NL2SQL 最重要的不是让模型“猜表名”，而是建立业务语义层。MySQL 中可以维护：

```text
指标：销售额
定义：订单实收金额，不含取消订单
口径：sum(paid_amount)
时间字段：paid_at
维度：区域、产品、客户、月份
权限：销售经理及以上
```

表和字段元数据被向量化后，可以用 BM25+向量检索找出可能的表、字段和指标，再把有限的 schema 给模型。上下文越精准，SQL 越可靠，也能减少 Prompt 长度。

### 6.5 NL2SQL 全流程模拟

用户问：

> “查询华东区域 2026 年第二季度各产品的实收销售额，按销售额从高到低排序。”

系统处理：

1. 识别为经营数据查询；
2. 抽取条件：区域=华东、时间=2026-04-01 至 2026-06-30、维度=产品、指标=实收销售额；
3. 检索指标元数据和授权表；
4. 生成 SQL；
5. 检查只读、表权限、时间范围和结果上限；
6. `EXPLAIN` 检查是否全表扫描或缺少索引；
7. 数据库执行；
8. 对结果做数值和空结果检查；
9. 生成表格、口径说明和结论。

合理 SQL 示例：

```sql
SELECT product_name, SUM(paid_amount) AS total_paid
FROM orders
WHERE region = '华东'
  AND paid_at >= '2026-04-01'
  AND paid_at < '2026-07-01'
  AND order_status = 'paid'
GROUP BY product_name
ORDER BY total_paid DESC
LIMIT 100;
```

为什么用小于 `2026-07-01`，而不是 `<= 2026-06-30`？因为时间字段可能包含时分秒，半开区间能覆盖 6 月 30 日全天。

### 6.6 SQL 校验和自动修正

`EXPLAIN` 只能帮助查看执行计划，不能证明 SQL 业务口径正确。系统至少要做四层校验：

- 语法校验：SQL 能否解析；
- 安全校验：只允许 SELECT，禁止写操作和多语句；
- 权限校验：只能访问授权表和字段；
- 语义校验：指标、时间、维度和过滤条件是否与问题一致。

执行失败时可以把数据库错误反馈给模型修正一次或两次，但不能无限重试。修正后的 SQL 必须重新做全部校验。

### 6.7 Agent 里的条件、循环和并行

- 条件：问题是知识问答还是数据查询；
- 循环：SQL 生成失败后修正，但设置最大次数；
- 并行：同时检索文档和业务指标元数据；
- 合并：把多路结果统一成可解释答案。

上下文隔离是指不同子任务使用自己的中间状态，最后只把必要结果合并，避免一个 Agent 的大量无关内容污染另一个 Agent。

### 6.8 评测集和 CI/CD 回归

约 1200 条企业业务测试集可以按类型分层：

- 意图识别；
- 知识库问答；
- 工具调用参数；
- NL2SQL 的 SQL 正确率；
- 数据结果正确率；
- 越权和危险问题。

每次修改 Prompt、检索、模型或 Agent 图后自动运行测试，比较新旧版本。如果整体准确率升高但危险 SQL 增多，也不能上线。CI/CD 回归的价值是防止修复 A 场景时破坏 B 场景。
