# Multi-Agent-Medical-Assistant 架构与数据流详解

> 本文基于仓库源码逐文件梳理，覆盖：进程模型、HTTP 接口契约、LangGraph 状态机、六大 Agent 内部数据流、数据库（Qdrant / docstore）存储结构与调用方式、文件生命周期、语音链路与人工审核链路。
> 文中标注 `文件:行号` 便于对照源码。

---

## 1. 进程与部署拓扑

```
┌──────────────────────────────────────────────────────────────┐
│ 浏览器 (templates/index.html, 单页 HTML/CSS/JS)               │
└───────────────┬──────────────────────────────────────────────┘
                │ HTTP (fetch, JSON / multipart-formdata)
┌───────────────▼──────────────────────────────────────────────┐
│ uvicorn 进程 (app.py 入口, host=0.0.0.0, port=8000)           │
│  ├─ FastAPI 主线程：7 个 HTTP 接口 + 2 个静态挂载              │
│  └─ 后台守护线程 cleanup_old_audio：每 300s 清空 uploads/speech│
└───────────────┬──────────────────────────────────────────────┘
                │ 进程内同步调用 process_query()
┌───────────────▼──────────────────────────────────────────────┐
│ LangGraph 状态机 (agents/agent_decision.py)                   │
│  每次请求新建图实例，但共享同一个 MemorySaver 记忆(thread_id=1)│
│  ├─ 安检节点 → 路由节点 → 6 个执行节点 → 审核节点 → 出口安检    │
│  └─ 执行节点内部再调：RAG 流水线 / Tavily / 本地 PyTorch 模型   │
└───────────────────────────────────────────────────────────────┘
```

**外部依赖（全部凭 .env 密钥访问）：**

| 依赖 | 用途 | 调用位置 |
|---|---|---|
| Azure OpenAI (GPT-4o) | 路由决策、闲聊、RAG 生成、查询扩展、切块、图像分类 | 全局各处 |
| Azure OpenAI Embedding (text-embedding-ada-002, 1536维) | 文档/查询向量化 | `vectorstore_qdrant.py` |
| Qdrant (本地文件模式 `data/qdrant_db`) | 稠密+稀疏混合向量库 | `vectorstore_qdrant.py` |
| Tavily | 联网搜索 | `tavily_search.py` |
| ElevenLabs | 语音转文字 / 文字转语音 | `app.py` |
| HuggingFace cross-encoder `ms-marco-TinyBERT-L-6` | 检索结果重排 | `reranker.py` |
| Google Drive (gdown) | 首次运行下载皮肤分割模型权重 | `model_download.py` |

---

## 2. HTTP 接口层（前端 ↔ 后端契约）

### 2.1 `GET /` — 首页
返回 `templates/index.html` 渲染的 HTML（Jinja2），无数据交互。

### 2.2 `POST /chat` — 纯文字提问
代码：`app.py:94`

```json
// 请求 (application/json)
{
  "query": "What is a brain tumor?",
  "conversation_history": []        // 前端永远传空数组；历史由后端 MemorySaver 维护
}
// Cookie: session_id=<uuid> （首次无 Cookie 时后端生成并 set_cookie）

// 响应 (application/json)
{
  "status": "success",
  "response": "<markdown 文本>",
  "agent": "RAG_AGENT"
  // 若 agent 为 "SKIN_LESION_AGENT, HUMAN_VALIDATION" 且分割图存在，额外返回：
  // "result_image": "/uploads/skin_lesion_output/segmentation_plot.png"
}
```

**内部处理链：** `process_query(request.query)` → 取 `response_data['messages'][-1].content` 作为回复文本 → 拼 JSON 返回。异常直接抛 HTTP 500。

### 2.3 `POST /upload` — 上传图片（可选带文字）
代码：`app.py:131`

```http
// 请求 (multipart/form-data)
image: <文件流, 文件名>
text:  ""                          // 可选表单字段
Cookie: session_id=<uuid>

// 校验链：
// 1) allowed_file()：扩展名 ∈ {png, jpg, jpeg}，否则 400
// 2) 大小 ≤ config.api.max_image_upload_size (5MB)，否则 413
// 3) 落盘：uploads/backend/<uuid4>_<secure_filename(原名)>
// 4) process_query({"text": text, "image": 文件绝对路径})
// 5) 处理完成后 os.remove(临时文件) —— 后端临时文件用完即删
```

响应结构与 `/chat` 相同；皮肤分割场景附带 `result_image`。

### 2.4 `POST /validate` — 人工审核结果回传
代码：`app.py:205`

```http
// 请求 (multipart/form-data)
validation_result: "yes" | "no"    // 必填
comments:          ""              // 可选
Cookie: session_id=<uuid>

// 后端把 "Validation result: yes Comments: ..." 作为新问题
// 再次走完整 process_query() 流程（见 §5.6 的审核闭环）
// 响应：
{ "status": "validated",   "message": "**Output confirmed by human validator:**", "response": "<AI文本>" }
// 或
{ "status": "rejected",    "comments": "<医生意见>", "message": "**Output requires further review:**", "response": "<AI文本>" }
```

### 2.5 `POST /transcribe` — 语音转文字
代码：`app.py:244`

```http
// 请求：multipart，audio=<录音 blob(.webm)>
// 处理链：
//   uploads/speech/speech_<uuid>.webm 落盘
//   → pydub 转 mp3 (speech_<uuid>.mp3)
//   → 读成字节流
//   → client.speech_to_text.convert(file=..., model_id="scribe_v1",
//        tag_audio_events=True, language_code="eng", diarize=True)
//   → 两个临时文件删除
// 响应：{"transcript": "<识别文本>"}
```

### 2.6 `POST /generate-speech` — 文字转语音
代码：`app.py:327`

```json
// 请求
{ "text": "<要朗读的文本>", "voice_id": "21m00Tcm4TlvDq8ikWAM" }

// 后端直连 ElevenLabs：
// POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}/stream
// model_id="eleven_monolingual_v1", stability=0.5, similarity_boost=0.5
// 响应：FileResponse(audio/mpeg) 二进制流 → 前端 URL.createObjectURL 播放
```

### 2.7 `GET /health`
返回 `{"status":"healthy"}`，供 Docker HEALTHCHECK 探活。

### 2.8 静态文件挂载
- `/data/*` → `data/` 目录（参考文档 PDF、解析出的图片等，供 RAG 答案中的"来源链接"使用）
- `/uploads/*` → `uploads/` 目录（皮肤分割结果图）

---

## 3. LangGraph 状态机（agent_decision.py）

### 3.1 State 结构（AgentState, agent_decision.py:85）

每一次用户请求都是一个"状态包"，在图的节点之间传递、修改：

| 字段 | 类型 | 写入者 | 用途 |
|---|---|---|---|
| `messages` | List[BaseMessage] | 入口/各节点 | 会话消息记录（持久化在 MemorySaver） |
| `current_input` | str \| dict | 入口 | 本次输入：纯文本 或 `{"text","image"}` |
| `agent_name` | str | 路由/各执行节点 | 当前处理的 Agent 名（可叠加逗号串联） |
| `has_image` | bool | analyze_input | 本次输入是否带图片 |
| `image_type` | str | analyze_input | 视觉模型判定的图片类型 |
| `output` | str \| AIMessage | 各执行节点 | 最终要发给用户的文本 |
| `needs_human_validation` | bool | 影像节点 | 是否需要人工审核 |
| `retrieval_confidence` | float | RAG 节点 | RAG 检索信心分 |
| `bypass_routing` | bool | analyze_input | 安检未通过→跳过路由直接出口 |
| `insufficient_info` | bool | RAG 节点 | RAG 回答是否"信息不足" |

### 3.2 图结构与边（agent_decision.py:599-667）

```
                    ┌─────────────┐
    入口 ──────────▶│analyze_input│──(bypass_routing?)──┐
                    └──────┬──────┘                     │
                           │ 正常                        │ 安检拦截
                    ┌──────▼──────┐                      │
                    │route_to_agent│                     │
                    └───┬───┬───┬─┴──┬────┬─────┐       │
        置信<0.85 缺省   │   │   │    │    │     │       │
        (→RAG_AGENT)    ▼   ▼   ▼    ▼    ▼     ▼       │
                   CONV  RAG WEB  BRAIN CHEST SKIN      │
                   │     │   │     │    │    │          │
                   │     │◀──┘     │    │    │          │
                   │     │ (RAG低信心自动改道WEB)         │
                   └──┬──┘         └─┬──┘──┘──┘         │
                      │              │                   │
                 ┌────▼──────────────▼───┐               │
                 │ check_validation      │               │
                 │  needs_human_validation?             │
                 └────┬─────────┬────────┘               │
                  否 │         │ 是                      │
                     │    ┌────▼────────┐               │
                     │    │human_validation│            │
                     │    └────┬────────┘               │
                     │         │                        │
                 ┌───▼─────────▼───────┐                │
                 │ apply_guardrails    │◀───────────────┘
                 │ (出口安检/审核分支)   │
                 └───┬─────────────────┘
                     │
                    END
```

### 3.3 逐节点数据流

**① analyze_input（agent_decision.py:128）**
- 入：`current_input`
- 出：`has_image`、`image_type`、`bypass_routing`
- 逻辑：
  1. 取文本 → `guardrails.check_input(text)`，LLM 返回 `SAFE`/`UNSAFE:原因`；UNSAFE 时把拒绝话术写进 `messages`，`agent_name="INPUT_GUARDRAILS"`，`bypass_routing=True`
  2. 若输入是 dict 且含 `"image"` → `ImageAnalysisAgent.analyze_image(路径)` → GPT-4o 视觉模型返回 JSON `{"image_type":"CHEST X-RAY", "confidence":0.9}`

**② route_to_agent（agent_decision.py:177）**
- 入：`messages`（取最近 6 条拼上下文）、`current_input`、`has_image`、`image_type`
- 组装 decision_input → `decision_chain`（系统提示词 + GPT-4o + JsonOutputParser）→ JSON `{"agent","reasoning","confidence"}`
- 出：`agent_name`；`confidence < 0.85` 时强制走 RAG_AGENT
- 路由映射（agent_decision.py:55-79 提示词约束）：
  - 无图片且闲聊 → CONVERSATION_AGENT
  - 具体医学知识 → RAG_AGENT
  - 最新动态/疫情 → WEB_SEARCH_PROCESSOR_AGENT
  - 有图片按 image_type → 对应影像 Agent

**③ 各执行节点**：见 §4、§5。

**④ check_validation（agent_decision.py:516）**
- `needs_human_validation==True` → 走 human_validation 节点
- 否则 → apply_guardrails

**⑤ human_validation（agent_decision.py:522）**
- 在原 output 后追加 `**Human Validation Required:** ...` 话术
- `agent_name` 追加 `, HUMAN_VALIDATION`（前端据此显示 Yes/No 按钮）

**⑥ apply_guardrails（agent_decision.py:539）**
- 若 output 含 "Human Validation Required" 且当前输入以 yes/no 开头（即 /validate 重放的请求）：
  - "no" → 追加 HumanMessage(Validation Result) + AIMessage("需进一步复核…")
  - "yes" → 仅追加 HumanMessage
- 否则走 `guardrails.check_output(output, input)`，用 LLM 做出口安全审查（无免责声明的医疗建议会被改写）
- 最终 `output` 塞入 `messages`（即答案入历史）

---

## 4. 文字问答各 Agent 数据流

### 4.1 CONVERSATION_AGENT（agent_decision.py:231）
- 把全部历史 + 当前问题拼进一个 300 字长提示词（内含角色设定/伦理约束/示例）
- `config.conversation.llm`（温度 0.7）直接生成 → 输出 AIMessage
- 无外部工具调用

### 4.2 RAG_AGENT（agent_decision.py:324 → agents/rag_agent/）
`MedicalRAG.process_query()` 内部五步（rag_agent/__init__.py:162）：

```
query(字符串)
 ① QueryExpander.expand_query          → LLM 加医学术语/同义词 → expanded_query
 ② VectorStore.load_vectorstore        → 打开 Qdrant + docstore
    VectorStore.retrieve_relevant_chunks → hybrid 检索 top_k=5 块
        输出: [{id, content, score, source, source_path}]
 ③ Reranker.rerank                     → cross-encoder 打分
        输出: [{..., rerank_score, combined_score}×3] + picture_reference_paths
 ④ ResponseGenerator.generate_response → 上下文拼提示词 → GPT-4o 生成
        只按资料回答；答不出必须说固定句式（供 insufficient_info 检测）
        输出: {response(附来源链接+参考图链接), sources, confidence}
 ⑤ 返回给 agent_decision.py:
        confidence < 0.40 (config.rag.min_retrieval_confidence) → output 置空
        或 文本含"don't have enough information"等关键词 → insufficient_info=True
```

**信心分计算（response_generator.py:212）**：取前 3 块 `combined_score = (余弦相似度 + 重排分)/2` 的平均值。

**低信心/信息不足 → 自动改道 WEB_SEARCH（agent_decision.py:437）**，`agent_name` 变为 `"RAG_AGENT, WEB_SEARCH_PROCESSOR_AGENT"`（标注双 Agent 协作）。

### 4.3 WEB_SEARCH_PROCESSOR_AGENT（web_search_processor.py:48）
```
query + 最近20条历史
 ① LLM 把"问题+相关历史"压缩成一条适合搜索的问题
 ② TavilySearchResults(max_results=5).invoke(查询)   ← langchain_community 封装
        (pubmed_search.py 存在但被注释停用)
 ③ 结果文本(标题+URL+内容+评分) 拼提示词 → config.web_search.llm 生成总结回复
```

---

## 5. 影像 Agent 数据流

### 5.1 图片类型识别（image_classifier.py）
- 本地图片 → `local_image_to_data_url()`：读字节 → base64 → `data:{mime};base64,...`
- 作为 `image_url` 发给 GPT-4o 视觉 → JSON：`BRAIN MRI SCAN / CHEST X-RAY / SKIN LESION / OTHER / NON-MEDICAL`
- 判定结果决定路由到哪个影像 Agent

### 5.2 CHEST_XRAY_AGENT（covid_chest_xray_inference.py）
```
图片路径
 → PIL 读图 → Resize(150,150) → ToTensor → Normalize(ImageNet均值方差)
 → DenseNet121(自训练分类头: covid19/normal) 前向推理
 → argmax → "covid19" | "normal"
 → 映射话术: POSITIVE/NEGATIVE 文本
 → needs_human_validation=True
模型权重: agents/image_analysis_agent/chest_xray_agent/models/covid_chest_xray_model.pth
```

### 5.3 SKIN_LESION_AGENT（skin_lesion_inference.py）
```
图片路径
 → 首次运行先 gdown 从 Google Drive 下载权重 checkpointN25_.pth.tar
 → cv2 读图 → BGR→RGB → /255 归一化 → Resize(256,256)
 → U-Net(自实现, 3→64→128→256→512→1024→上采样, n_classes=1) 推理
 → 输出 mask resize 回原图尺寸
 → matplotlib 把 mask(alpha=0.4) 叠加到原图
 → 保存 uploads/skin_lesion_output/segmentation_plot.png
 → needs_human_validation=True
前端通过 result_image 字段拿到分割图 URL，与原图并排展示
```

### 5.4 BRAIN_TUMOR_AGENT
`brain_tumor_inference.py` 内容为 `# TBD`，`agent_decision.py:450` 节点存在但只返回占位话术（"This would be handled by the brain tumor agent..."），未接模型。

### 5.5 影像结果的前端确认
- 响应 `agent` 字段含 `HUMAN_VALIDATION` → 前端渲染 Yes/No 按钮（index.html:1138）
- 按钮点击 → `sendValidation()` → POST /validate（index.html:1425）

### 5.6 审核闭环（人→机器→人）
```
POST /validate (validation_result, comments)
 → app.py 拼 "Validation result: yes Comments: ..." 
 → 再次 process_query() 走完整图
 → apply_guardrails 节点识别 yes/no 前缀，把结果写回 messages
 → 返回 {status: validated/rejected, response: <AI最终话术>}
 → 前端渲染 "HUMAN_VALIDATED" 气泡
```

---

## 6. 数据库与存储详解

### 6.1 Qdrant 向量库（本地文件模式）

配置（config.py:54-110）：

```python
vector_db_type = "qdrant"
embedding_dim = 1536              # text-embedding-ada-002
distance_metric = "Cosine"
vector_local_path = "./data/qdrant_db"      # Qdrant 落盘位置
collection_name = "medical_assistance_rag"
chunk_size = 512, chunk_overlap = 50        # 注：实际切块由 LLM 语义切块主导
top_k = 5, vector_search_type = 'similarity'
reranker_model = "cross-encoder/ms-marco-TinyBERT-L-6"
reranker_top_k = 3
```

**Collection schema（vectorstore_qdrant.py:43）：**
- 双向量设计：
  - `dense`：1536 维稠密向量（Azure embedding，COSINE）
  - `sparse`：稀疏向量（FastEmbedSparse `Qdrant/bm25`）
- 检索模式 `RetrievalMode.HYBRID`（LangChain 内部融合两种打分）
- 每个 point 的 payload：`{page_content: 块文本, source: 源文件名, doc_id: uuid, source_path: http://localhost:8000/...}`

### 6.2 docstore（父文档存储）

- 实现：`langchain.storage.LocalFileStore(data/docs_db)`，key=doc_id，value=块文本的 UTF-8 字节
- 作用：向量库只存 embedding，检索命中后用 doc_id 回 docstore 取原文（parent document 模式）
- 仓库里 `data/docs_db/` 下的 uuid 文件即为已入库块的原文

### 6.3 目录结构与文件生命周期

| 路径 | 内容 | 生命周期 |
|---|---|---|
| `data/raw/`, `data/raw_extras/` | 待入库 PDF（脑瘤/新冠X光/皮肤病灶/糖尿病论文） | 手动放入 |
| `data/parsed_docs/` | Docling 解析出的页图、表格图、插图（`<文件名>-picture-N.png`） | 入库时生成，RAG 答案的"参考图"链接指向这里 |
| `data/qdrant_db/` | Qdrant 本地库 | 持久 |
| `data/docs_db/` | 块原文 docstore | 持久 |
| `uploads/backend/` | 后端收到的原图（uuid 前缀命名） | 处理后立即删除 |
| `uploads/frontend/` | 前端预览用（当前未在后端代码中使用） | 长期 |
| `uploads/skin_lesion_output/` | `segmentation_plot.png` 分割结果 | 每次分割覆盖，供 result_image 引用 |
| `uploads/speech/` | 语音临时文件 | 守护线程每 5 分钟清空 |

### 6.4 RAG 入库管线（ingest_rag_data.py → MedicalRAG.ingest_file）

```
python ingest_rag_data.py --file <pdf> | --dir <目录>
 ① MedicalDocParser.parse_document (doc_parser.py:24)
    Docling DocumentConverter：PDF → 结构化文档
    提取：每页 PNG、TableItem 表格图、PictureItem 插图
    输出: (parsed_document, [图片 URI 列表])
 ② ContentProcessor.summarize_images (content_processor.py:24)
    每张图 → GPT-4o 视觉摘要；非信息性图片返回 'non-informative'
 ③ ContentProcessor.format_document_with_images
    export_to_markdown（图片→占位符）→ 用摘要替换占位符
    摘要文本以 "picture_counter_N <摘要>" 形式写入正文
 ④ ContentProcessor.chunk_document (content_processor.py:129)
    先按 "\n#" 标题边界预切 → 打 <|start_chunk_i|> 标记
    → chunker 模型(温度0.0) 建议合并/切分点 "split_after: 3, 5"
    → 按建议合并成 256~512 词的语义块
 ⑤ VectorStore.create_vectorstore (vectorstore_qdrant.py:90)
    每块: uuid → Document(page_content, metadata{source, doc_id, source_path})
    → 双向量嵌入 → qdrant_vectorstore.add_documents()
    → 原文字节写入 docstore.mset()
    集合不存在时自动 create_collection()
```

**查询管线（对称反向）**：见 §4.2，检索阶段用同一 `load_vectorstore()` 打开既有库。

---

## 7. 配置与环境变量

config.py 按职责拆 9 个配置类，全部从 `.env` 读取：

| 配置类 | 关键项 |
|---|---|
| AgentDecisoinConfig | 路由 LLM，温度 0.1 |
| ConversationConfig | 闲聊 LLM，温度 0.7 |
| WebSearchConfig | 搜索总结 LLM，温度 0.3；历史 20 条 |
| RAGConfig | Qdrant 参数、embedding、reranker、top_k、min_retrieval_confidence=0.40、max_context_length=8192 |
| MedicalCVConfig | 三个模型权重路径、分割输出路径、视觉 LLM 温度 0.1 |
| SpeechConfig | ElevenLabs key + 默认音色 |
| ValidationConfig | 各 Agent 是否需要人工审核（3 个影像 Agent=True）、超时、default_action=reject |
| APIConfig | host/port/最大图片 5MB |
| UIConfig | 语音/图片上传开关 |

必需环境变量：`deployment_name, model_name, azure_endpoint, openai_api_key, openai_api_version, embedding_deployment_name, embedding_model_name, embedding_azure_endpoint, embedding_openai_api_key, embedding_openai_api_version, ELEVEN_LABS_API_KEY, TAVILY_API_KEY, HUGGINGFACE_TOKEN`（QDRANT_URL/KEY 仅服务器版需要）。

---

## 8. 端到端时序（两种典型请求）

### 8.1 文字提问 "什么是脑肿瘤？"

```
浏览器 ──POST /chat {query}──▶ app.py ──process_query()──▶ LangGraph
  analyze_input: 安检LLM→SAFE
  route_to_agent: GPT-4o→{"agent":"RAG_AGENT","confidence":0.95}
  RAG_AGENT: 扩展查询→Qdrant混合检索5块→docstore取原文→
             cross-encoder重排3块→GPT-4o按资料生成(附来源链接)
             信心分0.72 ≥ 0.40 → 不回退
  check_validation: 无需人工审核
  apply_guardrails: 出口安检→改写入messages
◀── 返回 {status, response(markdown), agent:"RAG_AGENT"} ── 前端渲染气泡
```

### 8.2 上传皮肤病灶图

```
浏览器 ──POST /upload (multipart)──▶ app.py
  校验类型/大小 → 落盘 uploads/backend/<uuid>.jpg
  process_query({text, image})
    analyze_input: 安检 + GPT-4o视觉→"SKIN LESION"
    route_to_agent: → SKIN_LESION_AGENT
    SKIN_LESION_AGENT: U-Net推理→叠加图→uploads/skin_lesion_output/segmentation_plot.png
                       needs_human_validation=True
    human_validation: 输出+"Human Validation Required"话术
    apply_guardrails: 出口安检
  app.py 检测 agent 含 SKIN_LESION_AGENT,HUMAN_VALIDATION
    → 附 result_image 字段；删除 uploads/backend 临时文件
◀── {status, response, agent:"SKIN_LESION_AGENT, HUMAN_VALIDATION", result_image} ──
前端显示原图|分割图并排 + Yes/No 按钮
用户点 Yes ──POST /validate──▶ 重走 process_query("Validation result: yes")
  → apply_guardrails 识别 yes → 结果入历史
◀── {status:"validated", message, response} ── 前端渲染 "HUMAN_VALIDATED" 气泡
```

---

## 9. 关键代码索引

| 关注点 | 位置 |
|---|---|
| HTTP 接口定义 | `app.py:84-394` |
| 图构建与节点 | `agents/agent_decision.py:106-667` |
| 查询入口 | `agents/agent_decision.py:686` |
| 路由提示词 | `agents/agent_decision.py:55` |
| 双安检 | `agents/guardrails/local_guardrails.py` |
| RAG 编排 | `agents/rag_agent/__init__.py:162` |
| 入库编排 | `agents/rag_agent/__init__.py:107` |
| Qdrant 读写 | `agents/rag_agent/vectorstore_qdrant.py` |
| 重排 | `agents/rag_agent/reranker.py:33` |
| 生成与信心分 | `agents/rag_agent/response_generator.py:89` |
| 图像分类 | `agents/image_analysis_agent/image_classifier.py:36` |
| X光推理 | `agents/image_analysis_agent/chest_xray_agent/covid_chest_xray_inference.py:56` |
| 皮肤分割 | `agents/image_analysis_agent/skin_lesion_agent/skin_lesion_inference.py:113` |
