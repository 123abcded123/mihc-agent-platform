# MIHC Agent · 客户多组学 mIHC 科研服务平台 — 架构与数据流详解

> 本文以**当前仓库源码**为准，覆盖：部署拓扑、HTTP 接口契约、mIHC 分支路由、LangGraph 编排、
> 确定性表格分析、文献自动入库、检索链路（ES/BM25 + Milvus + RRF + Reranker）、
> 存储结构（PostgreSQL/SQLite、Redis/fakeredis、MongoDB/JSON、对象存储）与鉴权模型。

---

## 1. 进程与部署拓扑

```
┌─────────────────────────────────────────────────────────────┐
│ 浏览器（Vue3 + Element Plus，frontend/src/App.vue）          │
│ 侧边会话栏 / 项目工作台 / 知识库管理（含文献自动下载）        │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTPS（Bearer token 鉴权）
┌──────────────────────▼──────────────────────────────────────┐
│ uvicorn 进程（app.py，默认 API_PORT=8000，Sealos 上 3000）   │
│  ├─ FastAPI 路由：/api/v1/chat、auth、projects、files、      │
│  │   analysis、literature、documents、sessions、badcases     │
│  ├─ CORS 中间件（MIHC_CORS_ORIGINS）                         │
│  └─ 后台线程：模型预热（embedding/reranker 懒加载）           │
└──────────────────────┬──────────────────────────────────────┘
                       │ 同步调用
        ┌──────────────┼───────────────────────────┐
        ▼              ▼                           ▼
  mIHC 分支路由    LangGraph 编排              确定性分析/文献
  mihc/routing.py  agents/graph.py             mihc/table_analysis.py
  （澄清/人工/领域）  （安检→意图→规划→Agent→  mihc/literature.py
                      合并→引用检查→输出护栏）
        │              │                           │
        ▼              ▼                           ▼
  通用检索链 rag/retriever/hybrid_retriever.py（各 Agent 共用）
  ES/BM25 + Milvus(BGE-M3) 双路召回 → RRF 融合 → BGE-Reranker/得分降级
```

**Sealos 部署拓扑（当前线上）：**

| 组件 | 端口 | 公网地址 | 说明 |
|---|---|---|---|
| 前端 DevBox `medical-agent` | 3000 | `https://fgditvzngxkq.sealoshzh.site` | Vite 构建产物 + `vite preview` |
| 后端 DevBox `medical-agent-post` | 3000 | `https://buzoppztbipv.sealoshzh.site` | uvicorn（`scripts/devbox_run.sh`） |
| MongoDB `test-db-mongodb` | 27017 | 无（仅内网） | badcase/评测数据 |

**外部依赖（.env 驱动，均有本地回退）：**

| 依赖 | 用途 | 回退 |
|---|---|---|
| LLM 网关（OpenAI 兼容 `/v1`） | 意图/规划/生成/引用检查/护栏 | 无（配置多个候选端点故障切换） |
| 嵌入端点（`EMBEDDING_PROVIDER=openai_compatible`） | 查询/文档向量化 | `local_bge_m3`（sentence-transformers） |
| PubMed E-utilities / PMC OA | mIHC 文献检索与开放获取下载 | — |
| PostgreSQL | 业务元数据/审计 | SQLite（`DATABASE_URL=sqlite:///...`） |
| Redis | 会话上下文 | fakeredis（进程内） |
| MongoDB | badcase/评测 | JSON 文件（`data/mongo_json`） |
| Milvus | 向量库 | milvus-lite（本地文件） |
| Elasticsearch | 关键词检索 | 本地 BM25（rank_bm25 + JSON） |

---

## 2. HTTP 接口契约（`app.py`）

### 2.1 鉴权接口

| 接口 | 说明 |
|---|---|
| `POST /api/v1/auth/register` | 注册（`MIHC_ALLOW_REGISTER=1` 时开放），PBKDF2 存哈希 |
| `POST /api/v1/auth/login` | 登录，返回 HMAC-SHA256 签名 token（24h） |
| `GET /api/v1/auth/me` | 当前用户信息（需 token） |

`services/auth.py`：token 格式 `base64url(payload).hmac_signature`，密钥 `MIHC_AUTH_SECRET`；
首次启动可按 `MIHC_ADMIN_USER/MIHC_ADMIN_PASSWORD` 幂等创建管理员。

### 2.2 对话主入口 `POST /api/v1/chat`（公开，可带 token）

```json
// 请求
{ "query": "比较治疗组和对照组 CD8 阳性率", "session_id": null,
  "project_id": "P-xxxx（可选，关联客户项目编号）", "file_ids": [] }

// 响应（ChatResponse）
{ "answer": "...", "session_id": "…", "citations": [...], "risks": [...],
  "trace_id": "…", "agent_chain": ["mihc_router"], "agent_traces": [...],
  "branch": "table_analysis", "workflow_status": "clarifying", "classification": {...} }
```

携带 Bearer token 时，`user_id/tenant_id` 取自 token；带 `project_id` 时校验项目成员
（匿名仅标注不阻断），项目上下文随状态传入编排。

### 2.3 项目工作台（全部需登录）

| 接口 | 说明 |
|---|---|
| `POST /api/v1/projects` | 创建项目（自动成为 owner/member） |
| `GET /api/v1/projects` | 我参与的项目列表 |
| `GET /api/v1/projects/{id}` | 项目详情 + 样本 + marker + 文件 |
| `POST /api/v1/projects/{id}/samples` | 添加样本（sample_id/group/batch/type） |
| `POST /api/v1/projects/{id}/markers` | 添加 marker（name/channel/antibody/threshold/unit） |
| `POST /api/v1/projects/{id}/files` | 上传客户文件（对象存储 + sha256 + kind） |
| `POST /api/v1/projects/{id}/analysis/table` | 确定性表格分析（见 §4） |
| `GET /api/v1/analysis-runs/{run_id}` | 查询分析运行状态与结果 |

### 2.4 文献自动入库 `POST /api/v1/literature/ingest`（需登录）

```json
// 请求
{ "query": "mIHC tumor immune microenvironment", "max_results": 10, "max_download": 5 }
// 响应
{ "status": "completed", "downloaded": 3, "ingested": 2, "details": [
  { "file": "PMC1234567.pdf", "status": "ingested", "doc_id": "…", "chunks": 12 } ] }
```

### 2.5 知识库 / 会话 / 运维（需登录）

| 接口 | 说明 |
|---|---|
| `POST /api/v1/documents/ingest` | PDF/DOCX/MD/TXT 入库 |
| `GET /api/v1/documents` | 已入库文档列表 |
| `GET/DELETE /api/v1/sessions/{id}` | 会话历史 / 清空 |
| `GET /api/v1/badcases`、`GET /api/v1/eval/results` | badcase 闭环 / 评测结果 |
| `GET /health`、`GET /metrics` | 探活 / Prometheus 指标（公开） |

---

## 3. 对话数据流（`services/service.py → agents/graph.py`）

### 3.1 mIHC 分支路由（先于通用编排执行）

```
ChatRequest → MIHCPlatform.chat
  → MihcIntentClassifier.classify（规则基线，输出 intent/confidence/entities/
    required_inputs/risk_level，可由 BERT 微调模型替换）
  → choose_branch（受控节点名，模型不能拼接节点名）：
      high risk            → human_review（拒绝/转人工）
      confidence < 0.75    → clarify（澄清，不调用业务工具）
      table_analysis 无文件 → clarify_missing_file
      image_analysis  无文件 → clarify_missing_image
      report_generation 无前置 → run_prerequisites
      experiment_design 无项目 → clarify_missing_project
      other               → general（通用对话）
      其余                 → product_consult / experiment_design / literature_recommendation
```

澄清/人工分支**不进入 LangGraph**，直接返回带 `branch/workflow_status/next_action` 的
`ChatResponse` 并写审计（status=needs_input）。表格/图像/报告进入领域服务，
只有 product/experiment/literature/general 进入通用图编排。

### 3.2 LangGraph 状态机（`agents/graph.py`）

```
START → load_context（Redis 历史）
      → input_guard（DFA 明文 + Prompt 注入护栏；拦截→拒绝应答 END）
      → classify_intent（微调 BERT > 快速模式规则 > LLM 回退）
      → conversation（other 意图，带项目上下文）
      │   或 build_plan（TaskPlanner LLM 拆解 / 快速模式单步规则）
      → execute_step（循环：动态路由到专业 Agent，最大步数防死循环）
      → merge（按步骤合并、引用去重、风险排序）
      → citation_check（编号越界静态检查 + LLM 引用支撑检查）
      → output_guard（DFA 输出扫描 + LLM 护栏）→ END
```

专业 Agent 注册表（`graph.get_agents`）：`literature_agent`、`knowledge_agent`、
`data_analysis_agent`（统计方法推荐）、`experiment_agent`（方案生成 + 对照/重复数/终点校验）。
所有 Agent 经 `BaseAgent.run`：工具超时、失败重试 ≤2 次、轨迹写入 `agent_traces`。

### 3.3 生成与安全

- 证据约束生成（`agents/base.py`）：只用检索片段、标注引用编号、证据不足明说、
  医疗建议与科研建议分开表达。
- 输出护栏（`security/prompt_guard.py`）：DFA 明文扫描 → LLM 审查（越权/临床化措辞/隐私）。
- 审计（`infra/postgres.py audit_logs`）：trace_id、意图、agent_chain、耗时、安全事件；
  被拦截/失败请求同时写 MongoDB badcase（采集-分析-优化-验证闭环）。
- 观测：OTel span（console/OTLP）、Prometheus 指标、Langfuse（三密钥齐全才启用）。

---

## 4. 表格分析数据流（确定性，模型不参与计算）

```
上传 cells.csv / omics.csv → 对象存储（data/objects/projects/{pid}/{fid}/…）
  → project_files 记录（sha256、kind、上传者）
分析请求 → require_project（租户+成员）→ 文件归属校验
  → create_analysis_run（status=running，记录 input_refs/模型版本）
  → load_table（csv 多编码回退 / tsv / xlsx，行数上限）
  → validate_cell_table（mihc/table_analysis.py）：
      必填列 sample_id/cell_id/group → 样本与项目元数据对齐（禁止按行号猜测）
      → group 与项目样本一致 → 重复 cell_id → marker 数值类型
      → 阈值：项目 marker 配置 > 二值列自动 0.5 > 否则报"未配置阈值"
      输出结构化质量报告：passed_checks / failed_checks / errors / warnings
  → summarize_cell_groups：先按样本聚合阳性率，再按组聚合（避免把细胞行当重复）
  → 组间 Welch t 检验 p_value（scipy）
  → summarize_omics：sample_id 对齐分组 → feature 均值 → 组间比较
  → analysis_run 落状态：completed / data_invalid（校验不过，next=clarify）
    / tool_failed（临时故障，next=retry）
响应 WorkflowResponse：status / branch / analysis_run_id / input_refs / output_refs /
  data（sample_count、marker_results、omics_results、quality_report、limitations）/ errors
```

原则：原始文件永远保留（对象存储），只有 `status=passed` 的规范化结果进入后续
报告生成；报告节点只消费已验证的 `analysis_run`，不补齐不存在的统计结果。

---

## 5. 文献自动下载入库（`mihc/literature.py`）

```
PubMed esearch（按检索词取 Top-N PMID，relevance 排序）
  → efetch（PMID → PMCID 映射）
  → PMC oa.fcgi（获取开放获取 PDF 链接）
  → 下载 PDF → data/literature/PMCxxxx.pdf（限速 0.4s/请求）
  → IngestionPipeline.ingest：解析（Docling，缺失时回退 pypdf/python-docx）
    → 清洗 → 按标题/段落切分 → 嵌入 → Milvus + BM25 双写 → PG 元数据
```

命令行走 `scripts/download_mihc_literature.py`（支持 `--no-ingest` 只下载）。

---

## 6. 通用检索链（`rag/retriever/hybrid_retriever.py`）

```
多路查询（原问题 + Query Rewrite + HyDE，原问题始终保留）
  → 双路召回：Milvus 向量（先权限过滤 tenant/doc_id）+ ES/BM25 关键词
  → RRF 融合（score = Σ 1/(k+rank)，k=60）
  → 重排：BGE-Reranker（本地模型）；云端/未启用时降级为 RRF 得分排序
  → Top-K 上下文（默认 5）
故障降级：Milvus 不可用 → 仅 BM25 路；重排不可用 → 得分排序；
        嵌入端点不可用 → 同左（不阻断检索链路）
```

---

## 7. 存储结构

### 7.1 PostgreSQL（`DATABASE_URL`，默认 SQLite 回退，`infra/postgres.py`）

| 表 | 用途 |
|---|---|
| `users` | 用户名、PBKDF2 密码哈希、租户、角色 |
| `projects` | 项目名、租户、owner、成员（JSON）、状态 |
| `samples` | sample_id、项目、group/batch/type |
| `markers` | marker 名、通道、抗体、阳性阈值、单位 |
| `project_files` | 文件元数据：object_key、sha256、kind、大小、上传者 |
| `analysis_runs` | 分支、状态机状态（received/running/validated/completed/data_invalid/tool_failed）、input_refs/output_refs、result_json、errors_json、next_action |
| `reports` | 报告元数据（模板版本、对象键、审核状态） |
| `documents` / `chunks` | 知识库文档版本/权限/片段索引 |
| `permissions` | 文档级权限 |
| `audit_logs` | 请求审计（trace/意图/agent_chain/耗时/安全事件） |
| `badcases` | badcase 闭环（阶段/反馈/专家标注/归因） |

### 7.2 Redis（`infra/redis_store.py`，fakeredis 回退）

`key = sess:{session_id}` → JSON：最近 N 条消息、当前意图、任务进度，TTL 过期；
长期记忆走 Milvus（语义检索）。

### 7.3 Milvus（`MILVUS_URI` 为空时 milvus-lite 本地文件）

collection `mihc_knowledge`：`id(=chunk_id), text, title, source, version,
tenant_id, doc_id, embedding(BGE-M3/远程端点)`

### 7.4 关键词库（`ES_URL` 为空时本地 BM25 JSON）

与 Milvus chunk 一一对应（同 chunk_id），供 RRF 融合；`data/bm25_index.json`。

### 7.5 MongoDB / JSON（`infra/mongo_store.py`）

集合：`badcases`、`eval_results`、`eval_datasets`；无服务时 `data/mongo_json/*.json`。

### 7.6 对象存储（`mihc/object_store.py`）

本地目录 `MIHC_OBJECT_STORAGE_DIR`（生产可换 S3/MinIO 实现）：
`projects/{project_id}/{file_id}/{file_name}`，路径穿越防护，写入时计算 sha256。

---

## 8. 前端数据流（`frontend/src/App.vue`）

- **会话**：localStorage 持久化 `[{id, title, serverSessionId, messages}]`，
  侧边栏切换/删除；首次聊天由后端返回 `session_id` 并保存，后续请求带同一
  session 延续后端上下文。
- **项目关联**：登录后加载项目列表，输入框下拉选择 → `chat(query, sessionId, projectId)`
  → 后端校验并注入项目上下文。
- **项目工作台**：建项目 → 加样本/marker → 上传表格（选 kind）→ 勾选文件发起
  分析 → 展示 WorkflowResponse 质量报告与统计结果。
- **知识库**：手动上传文档入库；或输入 PubMed 检索词一键文献下载入库并展示明细。
- **鉴权**：token 存 localStorage，`api.js` 自动附加 `Authorization: Bearer`；
  401 时引导重新登录。
