# MIHC 医疗科研智能 Agent 平台 — 重构方案

> 依据《项目文档.md》第 5 节「项目一：医疗科研领域智能 Agent 平台」与《简历李明珠.docx》项目经验，将现有 Multi-Agent-Medical-Assistant 重构为面向医疗科研服务（MIHC：医学信息与健康计算）的智能 Agent 平台。

## 1. 目标与范围

### 1.1 目标
- 服务对象：医疗科研人员（查知识、找文献、解读检验、设计实验、分析科研数据）
- 形态：**MIHC 科研服务的一个 Agent**，受控系统（意图识别 → 对应知识/工具 → 有依据的结果 → 可评估、可追踪）
- 不是训练"全知模型"，而是建立受控、可引用、可审计的科研助手

### 1.2 端到端流程（对齐文档 5.2）
```
Vue3 对话页面
  -> FastAPI 接口
  -> 身份/权限/敏感信息检查
  -> Redis 读取会话上下文
  -> BERT 意图识别：文献检索 / 知识问答 / 检验解读 / 数据分析 / 实验设计
  -> LangGraph 动态路由
  -> Query Rewrite / HyDE
  -> Elasticsearch 关键词检索 + Milvus 向量检索
  -> RRF 融合 + BGE-Reranker 重排
  -> 文献 Agent 整理证据 / 知识问答 Agent / 检验解读 Agent / 数据分析 Agent / 实验设计 Agent
  -> Qwen3-32B 生成最终回答
  -> DFA + Prompt Guardrail 安全检查
  -> Langfuse / OpenTelemetry 记录链路
  -> 返回答案、引用、风险和 trace_id
```

## 2. 技术栈映射与本地运行策略

| 文档要求 | 生产实现 | 本机（无 Docker/无服务器）回退 | 说明 |
|---|---|---|---|
| BERT 意图识别 | BERT 微调模型（GPU 推理） | transformers + `bert-base-chinese`（CPU），支持加载本地微调权重 | `agents/intent/` |
| LangGraph | LangGraph 图编排 + Redis Checkpointer | 同左，fakeredis 回退 | `agents/graph.py` |
| Elasticsearch | `elasticsearch-py` 关键词检索 | 本地 BM25 索引（rank_bm25，JSON 持久化） | `rag/retriever/es_store.py` 双后端 |
| Milvus | Milvus Server（MILVUS_URI） | **milvus-lite**（pymilvus 内嵌，本地文件 `data/milvus_lite.db`） | `rag/retriever/milvus_store.py` |
| BGE-M3 嵌入 | BGE-M3 本地推理 / 推理服务端点 | sentence-transformers 本地（CPU） | `core/embeddings.py` |
| BGE-Reranker | BGE-Reranker 本地推理 | 同左 | `rag/retriever/reranker.py` |
| Qwen3-32B(LoRA) | vLLM OpenAI 兼容端点（LLM_BASE_URL） | 回退 OpenAI API（兼容协议） | `core/llm.py` 模型工厂 |
| Redis 会话/记忆 | redis-py | fakeredis（进程内，配置 USE_FAKE_REDIS=1） | `infra/redis_store.py` |
| PostgreSQL 文档元数据/审计 | SQLAlchemy + psycopg | SQLite 回退（同一套 ORM 代码） | `infra/postgres.py` |
| MongoDB（badcase/评测） | pymongo | JSON 文件存储回退 | `infra/mongo_store.py` |
| Langfuse | 可选启用（LANGFUSE_* 存在才启用） | 关闭 | `observability/langfuse_tracing.py` |
| OpenTelemetry | OTLP exporter | Console exporter | `observability/otel.py` |
| Prometheus / Grafana | prometheus-client `/metrics` + Grafana Dashboard | 提供 dashboard JSON 与 provisioning | `observability/metrics.py`, `deploy/` |
| Vue3 | Vite + Vue3 + Element Plus | 构建产物由 FastAPI 挂载 | `frontend/` |
| Harness 评测 | 自动评测流水线（HitRate/Recall/MRR/忠实性/引用/安全） | 同左，本地可运行 | `eval/` |
| LoRA/DPO 微调 | SFT/LoRA + DPO 脚本（1.5W 指令数据） | 提供数据集构建与训练脚本（需 GPU） | `finetune/` |
| Docling 文档解析 | 复用现有解析/切分能力 | 同左 | `rag/ingestion/` |

## 3. 目录结构

```
app.py                      # FastAPI 入口（重写）
config.py                   # 集中配置（重写，多后端适配）
.env                        # 环境变量（扩展）

core/                       # 核心基础设施
  config.py                 #   (配置聚合)
  llm.py                    #   模型工厂：vLLM(Qwen3-32B)/OpenAI 兼容
  embeddings.py             #   BGE-M3 本地 / API 端点
  models.py                 #   Pydantic 数据模型
  errors.py                 #   业务异常

infra/
  postgres.py               # SQLAlchemy：文档元数据/版本/权限/审计日志
  redis_store.py            # 会话上下文/任务进度（fakeredis 回退）
  mongo_store.py            # badcase/评测样本（JSON 回退）

rag/
  ingestion/
    pipeline.py             # 入库管线：解析→清洗→切分→双写(ES+Milvus)→PG 元数据
    parser.py               # Docling 解析（复用 doc_parser 思路）
  retriever/
    hybrid_retriever.py     # 双路召回 + RRF 融合 + BGE-Reranker 重排
    milvus_store.py         # Milvus 写入/检索（milvus-lite 回退）
    es_store.py             # ES 关键词（本地 BM25 回退）
    reranker.py             # BGE-Reranker
  query_processor.py        # Query Rewrite + HyDE（保留原问，多路并行）

agents/
  intent/
    bert_intent.py          # BERT 意图分类（5+1 类）
  planner.py                # 任务规划：复杂问题拆解
  literature_agent.py       # 文献 Agent：检索证据 + 整理 + 引用
  knowledge_agent.py        # 知识问答 Agent（RAG）
  lab_interpret_agent.py    # 检验解读 Agent（临床标准知识库）
  data_analysis_agent.py    # 数据分析 Agent（工具调用）
  experiment_agent.py       # 实验设计 Agent（步骤拆解+方案校验）
  merge.py                  # 结果合并
  citation_check.py         # 引用检查
  graph.py                  # LangGraph 编排 + 动态路由
  state.py                  # 图状态定义

security/
  dfa.py                    # DFA 敏感词过滤（Aho-Corasick）
  prompt_guard.py           # Prompt 注入/输出侧护栏
  input_guard.py            # 输入侧敏感信息检测

observability/
  otel.py                   # OpenTelemetry 初始化
  tracing.py                # Langfuse/OTel 链路装饰器
  metrics.py                # Prometheus 指标

eval/                       # Harness 评测
  run_eval.py               # 评测入口
  datasets/                 # 评测集（JSONL）
  metrics_eval.py           # HitRate@K / Recall@K / MRR
  judge.py                  # 忠实性/完整性/引用正确率/安全拒答率
  README.md

finetune/                   # 微调（脚本，需 GPU）
  build_dataset.py          # 去重/脱敏/标注/切分
  sft_lora.py               # LoRA SFT
  dpo_train.py              # DPO
  README.md

frontend/                   # Vue3 + Vite + Element Plus
  src/
  vite.config.js
  package.json

deploy/
  docker-compose.yml        # ES/Milvus/Redis/PG/Mongo/Prometheus/Grafana/vLLM
  grafana/dashboards/agent-platform.json
  grafana/provisioning/...
```

## 4. 数据库设计

### 4.1 PostgreSQL（表结构，SQLAlchemy ORM）
| 表 | 用途 |
|---|---|
| `documents` | 文档版本、来源、权限（对齐文档 5.3 的 chunk 元数据） |
| `chunks` | chunk 索引（chunk_id, doc_id, title, text 摘要, embedding_model） |
| `permissions` | 文档/租户/用户权限 |
| `audit_logs` | 请求审计（user, session, intent, trace_id, 耗时, 安全事件） |
| `badcases` | badcase 闭环数据（对齐简历：采集-分析-优化-验证） |

### 4.2 Redis（键设计）
- `sess:{session_id}` → 最近 N 条消息、任务进度、当前意图
- `user:{user_id}` → 用户配置/权限缓存
- 每键 TTL，长期记忆走 Milvus（对齐文档 5.7）

### 4.3 Milvus（chunk schema）
`chunk_id, text, title, source, version, permissions, embedding(BGE-M3, 1024维), tenant_id, doc_id`

### 4.4 MongoDB（badcase/评测集合）
`badcases, eval_results, eval_datasets`

## 5. 多 Agent 编排（对齐文档 5.5）

```
入口 -> 输入安检(DFA+敏感信息) -> Redis 取上下文
  -> BERT 意图识别
  -> 任务规划（复杂问题拆解成可验证步骤）
  -> 动态路由（LangGraph conditional edges）:
       文献Agent / 知识问答Agent / 检验解读Agent / 数据分析Agent / 实验设计Agent
  -> 结果合并
  -> 引用检查（每个结论必须有来源；无证据=说明证据不足）
  -> 输出护栏（DFA + Prompt Guardrail）
  -> 输出（answer, citations, risks, trace_id）
```

约束：最大步数、工具超时、参数校验、失败重试（最多 2 次）、实验方案校验（对照组/重复数/终点指标）。

## 6. RAG 检索链（对齐文档 5.4）

1. **Query Rewrite + HyDE**：保留原始问题；并行生成关键词扩展 + 假设文档
2. **双路召回**：ES/BM25 关键词（药物/基因/指标/年份） + Milvus 向量（BGE-M3 语义）
3. **RRF 融合**：`score = Σ 1/(k + rank)`，k=60
4. **BGE-Reranker 重排** → Top-K（默认 5）
5. **生成**：证据约束 prompt（只用检索片段、给出引用、证据不足时明说、医疗建议与科研建议分开）

## 7. API 设计（新）

| 接口 | 说明 |
|---|---|
| `POST /api/v1/chat` | 对话主入口：`{query, session_id?}` → `{answer, citations[], risks[], trace_id, agent_chain[]}` |
| `GET /api/v1/sessions/{session_id}` | 会话历史 |
| `DELETE /api/v1/sessions/{session_id}` | 清空会话 |
| `POST /api/v1/documents/ingest` | 文档入库（multipart） |
| `GET /api/v1/documents` | 已入库文档列表（含版本/权限） |
| `POST /api/v1/eval/run` | 触发 Harness 评测 |
| `GET /health` `GET /metrics` | 探活 / Prometheus 指标 |

## 8. 实施阶段

- **P1 基础层**：目录骨架、config、core/（LLM 工厂、BGE-M3 嵌入）、infra/（PG/Redis/Mongo 回退）、app.py 新入口
- **P2 检索层**：rag/ 双路召回 + RRF + BGE-Reranker + Query Rewrite/HyDE + 入库管线
- **P3 智能体层**：BERT 意图、LangGraph 编排、5 个专业 Agent、结果合并、引用检查、DFA + 护栏
- **P4 观测与评测**：OTel/Langfuse/Prometheus、Harness 评测（HitRate/Recall/MRR/忠实性/引用/安全）
- **P5 前端与部署**：Vue3 前端、docker-compose、Grafana Dashboard、微调脚本、文档

## 9. 验证方式
- 每个阶段结束：应用可启动（`python app.py`），`/health`、`/api/v1/chat` 可访问
- P4 结束：`python eval/run_eval.py` 输出各指标
- 旧模块（影像分析/语音）保留在仓库但不再被主程序加载（避免启动加载大模型），后续可作为检验解读 Agent 的可选工具再集成
